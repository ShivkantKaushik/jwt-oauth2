spring.application.name=jwt-oauth2


spring.datasource.url=jdbc:postgresql://localhost:5432/shivkantkaushik
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true


#spring boot automatically converts kebab case (abc-def) to camel case abcDef
#thats why it is still working, even if we not have same variable name in record
jwt.rsa-private-key: classpath:certs/privateKey.pem
jwt.rsa-public-key: classpath:certs/publicKey.pem